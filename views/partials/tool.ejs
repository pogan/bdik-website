  <main class="container py-5" id="tool">
    <div class="tool-header d-flex flex-wrap justify-content-between align-items-end mb-3 gap-3">
      <div>
        <h2>Przeglądaj bazę</h2>
        <p class="small mb-0" id="result-count">Ładowanie danych…</p>
      </div>
    </div>

    <div class="filters-card p-3 mb-3">
      <div class="row g-2">
          <div class="col-6 col-md-3">
            <label class="form-label small text-secondary mb-1">Województwo</label>
            <select class="form-select form-select-sm" id="f-voivodeship">
              <option value="">Wszystkie</option>
            </select>
          </div>
          <div class="col-6 col-md-3">
            <label class="form-label small text-secondary mb-1">Powiat</label>
            <select class="form-select form-select-sm" id="f-county">
              <option value="">Wszystkie</option>
            </select>
          </div>
          <div class="col-6 col-md-3">
            <label class="form-label small text-secondary mb-1">Gmina</label>
            <select class="form-select form-select-sm" id="f-commune">
              <option value="">Wszystkie</option>
            </select>
          </div>
          <div class="col-6 col-md-3">
            <label class="form-label small text-secondary mb-1">Miejscowość</label>
            <select class="form-select form-select-sm" id="f-locality">
              <option value="">Wszystkie</option>
            </select>
          </div>
          <div class="col-6 col-md-3">
            <label class="form-label small text-secondary mb-1">Dane kontaktowe</label>
            <select class="form-select form-select-sm" id="f-contact">
              <option value="">Wszystkie rekordy</option>
              <option value="any">Tylko z kontaktem</option>
              <option value="email">Tylko z e-mailem</option>
              <option value="phone">Tylko z telefonem</option>
              <option value="website">Tylko ze stroną WWW</option>
            </select>
          </div>
          <div class="col-6 col-md-3">
            <label class="form-label small text-secondary mb-1">Typ instytucji</label>
            <select class="form-select form-select-sm" id="f-type">
              <option value="">Wszystkie typy</option>
              <% institutionTypes.forEach((t) => { %>
                <option value="<%= t.slug %>"><%= t.label %></option>
              <% }) %>
            </select>
          </div>
          <div class="col-12 col-md-9">
            <label class="form-label small text-secondary mb-1">Szukaj po nazwie</label>
            <div class="input-group input-group-sm">
              <span class="input-group-text"><i class="bi bi-search"></i></span>
              <input type="text" class="form-control" id="f-search" placeholder="np. biblioteka, dom kultury...">
              <button class="btn btn-outline-secondary" type="button" id="f-reset">Wyczyść filtry</button>
            </div>
          </div>
        </div>

        <!-- Panel filtrów administratora: po jednym polu na każdą filtrowalną
             kolumnę tabeli institutions. Wypełnia go table.js na podstawie
             window.BDIK_ADMIN_VIEW; dla pozostałych użytkowników nie ma go w HTML-u. -->
        <% if (adminView) { %>
        <div class="mt-3 pt-3 border-top" id="admin-filters">
          <button class="btn btn-sm btn-outline-secondary" type="button" data-bs-toggle="collapse"
                  data-bs-target="#admin-filters-body" aria-expanded="false" aria-controls="admin-filters-body">
            <i class="bi bi-sliders me-1"></i>Filtry administratora
            <span class="badge rounded-pill bg-secondary-subtle text-secondary-emphasis ms-1 d-none" id="admin-filters-count"></span>
          </button>
          <div class="collapse mt-3" id="admin-filters-body">
            <div class="row g-2" id="admin-filters-grid"></div>
          </div>
        </div>
        <% } %>
      </div>

    <section class="export-card mb-4" aria-labelledby="export-card-title">
      <div class="export-card-head">
        <div class="export-card-icon"><i class="bi bi-download"></i></div>
        <div>
          <h3 class="export-card-title" id="export-card-title">Pobierz wybrane dane</h3>
          <p class="export-card-sub mb-0">Wybierz format pliku — eksport obejmuje wszystkie wyniki z aktualnych filtrów.</p>
        </div>
      </div>
      <!-- Cena na żywo dla bieżących filtrów (wypełnia table.js z /api/institutions).
           Orientacyjna - wiążąca kwota i tak liczona jest na serwerze przy zakupie. -->
      <div class="export-price d-none" id="export-price">
        <i class="bi bi-tag-fill me-1"></i><span id="export-price-text"></span>
      </div>
      <div class="export-card-actions" id="export-buttons">
        <button type="button" class="btn export-btn" data-format="csv">
          <i class="bi bi-filetype-csv"></i><span>CSV</span>
        </button>
        <button type="button" class="btn export-btn" data-format="xlsx">
          <i class="bi bi-file-earmark-excel"></i><span>XLSX</span>
        </button>
        <button type="button" class="btn export-btn" data-format="pdf">
          <i class="bi bi-file-earmark-pdf"></i><span>PDF</span>
        </button>
      </div>
      <div class="export-hint small"><i class="bi bi-unlock me-1"></i>Pełne dane kontaktowe odblokowujesz eksportem · płacisz tylko za rekordy z kontaktem, resztę dostajesz gratis</div>
    </section>

    <div class="table-shell table-responsive">
      <table class="table table-hover align-middle mb-0" id="results-table">
        <thead>
          <tr id="table-head"></tr>
        </thead>
        <tbody id="table-body">
          <tr><td class="text-center text-secondary py-4">Ładowanie…</td></tr>
        </tbody>
      </table>
    </div>

    <nav class="d-flex justify-content-between align-items-center mt-3">
      <div class="text-secondary small" id="page-info"></div>
      <ul class="pagination pagination-sm mb-0" id="pagination"></ul>
    </nav>
  </main>
